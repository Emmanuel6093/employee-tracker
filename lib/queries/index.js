const view = require("./views")
const add = require("./add")
const update = require("./update")
const del = require("./delete")

module.exports = {
  view: view,
  add: add,
  update: update,
  delete: del,
} 